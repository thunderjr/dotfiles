{
  "name": "dotfiles",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "brew install colordiff",
    "generate": "bun run src/cli.ts generate",
    "sync": "bun run src/cli.ts sync"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
