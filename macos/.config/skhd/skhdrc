# skhdrc - skhd configuration file
# Place this file at ~/.config/skhd/skhdrc

# Switch to workspace 
alt - 1 : yabai -m space --focus 1
alt - 2 : yabai -m space --focus 2
alt - 3 : yabai -m space --focus 3
alt - 4 : yabai -m space --focus 4
alt - 5 : yabai -m space --focus 5
alt - 6 : yabai -m space --focus 6
alt - 7 : yabai -m space --focus 7
alt - 8 : yabai -m space --focus 8
alt - 9 : yabai -m space --focus 9

# Move window to workspace (like Hyprland's Super + Shift + 1-9)
alt + shift - 1 : yabai -m window --space 1 --focus
alt + shift - 2 : yabai -m window --space 2 --focus
alt + shift - 3 : yabai -m window --space 3 --focus
alt + shift - 4 : yabai -m window --space 4 --focus
alt + shift - 5 : yabai -m window --space 5 --focus
alt + shift - 6 : yabai -m window --space 6 --focus
alt + shift - 7 : yabai -m window --space 7 --focus
alt + shift - 8 : yabai -m window --space 8 --focus
alt + shift - 9 : yabai -m window --space 9 --focus

# Cycle through workspaces with comma and period
alt - 0x2F : yabai -m space --focus next
alt - 0x2B : yabai -m space --focus prev

# WINDOW NAVIGATION (HJKL - like vim/i3)
# Focus window in direction
alt - h : yabai -m window --focus west || yabai -m display --focus west
alt - j : yabai -m window --focus south || yabai -m display --focus south
alt - k : yabai -m window --focus north || yabai -m display --focus north
alt - l : yabai -m window --focus east || yabai -m display --focus east

# Move window in direction
alt + shift - h : yabai -m window --warp west
alt + shift - j : yabai -m window --warp south
alt + shift - k : yabai -m window --warp north
alt + shift - l : yabai -m window --warp east

# WINDOW RESIZING
# Resize windows (like i3's resize mode) - excluded from Ghostty
# cmd + alt - left [
#     "ghostty" : ~
#     * : yabai -m window --resize left:-50:0; yabai -m window --resize right:-50:0
# ]
# 
# cmd + alt - down [
#     "ghostty" : ~
#     * : yabai -m window --resize bottom:0:50; yabai -m window --resize top:0:50
# ]
# 
# cmd + alt - up [
#     "ghostty" : ~
#     * : yabai -m window --resize top:0:-50; yabai -m window --resize bottom:0:-50
# ]
# 
# cmd + alt - right [
#     "ghostty" : ~
#     * : yabai -m window --resize right:50:0; yabai -m window --resize left:50:0
# ]

# Toggle window zoom
alt - f : yabai -m window --toggle zoom-fullscreen

# Toggle window floating
alt + shift - space : yabai -m window --toggle float

# LAYOUT MANAGEMENT

# Toggle window split
cmd + alt - s : yabai -m window --toggle split

# Rotate layout clockwise
cmd + alt - r : yabai -m space --rotate 90

# Mirror layout
cmd + alt - y : yabai -m space --mirror y-axis
cmd + alt - x : yabai -m space --mirror x-axis

# Balance layout
cmd + alt - b : yabai -m space --balance

# MONITOR MANAGEMENT
# Focus monitor
cmd + alt - m : yabai -m display --focus next

# Move window to monitor
cmd + alt + shift - m : yabai -m window --display next; yabai -m display --focus next

# Launch terminal (like i3's terminal shortcut)
alt - return : open -n /Applications/Ghostty.app
